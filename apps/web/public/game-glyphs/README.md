# Game glyph assets

Monochrome game/category marks used by the tournament UI.

- Core games use cleaned monochrome silhouettes from the provided source logo files so the marks stay recognizable.
- Remaining games use compact Liquipedia-style glyphs designed for small badges.
- Components render these as CSS masks with `currentColor`, so the same asset works in dark, light, and themed UI.
